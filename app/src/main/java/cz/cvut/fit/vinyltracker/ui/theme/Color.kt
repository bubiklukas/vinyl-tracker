package cz.cvut.fit.vinyltracker.ui.theme

import androidx.compose.ui.graphics.Color

// App background — deep charcoal, slightly warm rather than pure black
val Background = Color(0xFF1A1A1A)

// Cards and list item surfaces
val Surface = Color(0xFF242424)

// Bottom sheets and elevated surfaces
val SurfaceHigh = Color(0xFF2E2E2E)

// Primary accent — Add FAB, Collection badge, gold highlights
val Gold = Color(0xFFD4A843)

// Wishlist accent and secondary actions
val Teal = Color(0xFF4EC9B0)

// Primary text — warm cream rather than pure white
val Cream = Color(0xFFF5F0E8)

// Secondary text, metadata, subtitles
val WarmMuted = Color(0xFFB8B0A0)

// Placeholder text, timestamps, inactive labels
val CharcoalMuted = Color(0xFF6E6860)

// Destructive actions and error states
val ErrorRed = Color(0xFFCF6679)

// Added confirmation and good condition badge
val SuccessGreen = Color(0xFF6BCB77)
